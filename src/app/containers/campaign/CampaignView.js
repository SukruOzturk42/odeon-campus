import React from "react";
import { injectIntl } from "react-intl";
import { Card } from "../../_metronic/_partials/controls/Card";
import { CardHeader, CardBody } from "../../_metronic/_partials/controls";

const CompanyView = (props) => {
  const { intl, activeSubMenu } = props;
  return <div>kampanya tanımlama sayfası </div>;
};

export default injectIntl(CompanyView);
