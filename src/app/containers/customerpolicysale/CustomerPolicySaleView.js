import React from "react";
import { injectIntl } from "react-intl";

const CustomerPolicySaleView = (props) => {
    const { intl, activeSubMenu } = props;
    return <div>Satış ödüllendirme </div>;
};

export default injectIntl(CustomerPolicySaleView);
