import React from "react";
import { injectIntl } from "react-intl";

const RuleView = (props) => {
    const { intl, activeSubMenu } = props;
    return <div>kural tanımlama sayfası </div>;
};

export default injectIntl(RuleView);