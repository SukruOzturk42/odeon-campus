import React from "react";
import { injectIntl } from "react-intl";

const CampaignsView = (props) => {
    const { intl, activeSubMenu } = props;
    return <div>kampanya listeleme sayfası </div>;
};

export default injectIntl(CampaignsView);