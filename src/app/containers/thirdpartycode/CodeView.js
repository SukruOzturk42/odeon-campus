import React from "react";
import { injectIntl } from "react-intl";

const CodeView = (props) => {
    const { intl, activeSubMenu } = props;
    return <div> Kod Oluşturma ve Yükleme Sayfası </div>;
};

export default injectIntl(CodeView);