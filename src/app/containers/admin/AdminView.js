import React from "react";
import { injectIntl } from "react-intl";

const AdminView = (props) => {
    const { intl, activeSubMenu } = props;
    return <div> Parametre Tanımlama </div>;
};

export default injectIntl(AdminView);